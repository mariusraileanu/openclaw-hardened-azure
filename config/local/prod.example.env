# Optional local-only prod overrides (not committed as .env)
# Copy to config/local/prod.env only if you need local machine overrides.

# COMPASS_API_KEY=replace-me
# TAVILY_API_KEY=
# SIGNAL_PROXY_AUTH_TOKEN=
# MSTEAMS_APP_SECRET_VALUE=
