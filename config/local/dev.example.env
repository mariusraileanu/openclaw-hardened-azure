# Optional local-only dev overrides (not committed as .env)
# Copy to config/local/dev.env only if you need local machine overrides.

# COMPASS_API_KEY=replace-me
# TAVILY_API_KEY=
# SIGNAL_PROXY_AUTH_TOKEN=
# OPENCLAW_BOARDS=fertility,strategic-health
