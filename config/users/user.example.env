# Per-user config template

# Optional Signal user phone
SIGNAL_USER_PHONE=

# Optional per-user secret overrides
COMPASS_API_KEY=
TAVILY_API_KEY=

# Optional per-user Teams toggle
MSTEAMS_ENABLED=false

# Optional advanced override for non-secret per-user capabilities.
# Prefer config/users/<slug>.features.json instead of setting this directly.
# OPENCLAW_FEATURES_JSON={}
