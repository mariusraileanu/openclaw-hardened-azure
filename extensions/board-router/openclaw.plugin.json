{
  "id": "board-router",
  "name": "Board Router",
  "description": "Registers the convene_board_meeting tool for deterministic board decision-making.",
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" },
      "boardIds": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  }
}
